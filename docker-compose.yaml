version: "3"

services:
  # MongoDB service
  mongo:
    container_name: mongo-db
    image: mongo:latest
    restart: always
    env_file:
      - ./server/.env
    volumes:
      - mongo-db:/data/db

  # Node API service
  api:
    container_name: node-api
    env_file:
      - ./server/.env
    build:
      context: ./server
      args:
        - SERVER_PORT=3000
    ports:
      - "3000:3000"
    volumes:
      - ./server:/app:ro
      - /app/node_modules
    depends_on:
      - mongo

volumes:
  mongo-db: {}
